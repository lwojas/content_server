{
  "name": "raycaster-content-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Content server for the raycaster game editor and runtime",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
